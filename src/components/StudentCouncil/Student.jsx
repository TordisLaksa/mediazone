export default function Student(props) {
    
    return (
     
            <>
                <h3 className="name">{props.name}</h3>
                <p className="email">{props.email}</p>
            </>
        )

}